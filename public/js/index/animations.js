// AOS animation and Lucide icon initialization.
    AOS.init({
        duration: 700,
        once: true,
    });
    // ================================
// Initialize Lucide Icons
// ================================
lucide.createIcons();
